## We fit y.bar = r(x.bar) + e. The variances of x.bar and y.bar can
## be computed based on multiple samples (large number of profiles in
## gene expression data).  By CLT, we assume normal errors and use 

